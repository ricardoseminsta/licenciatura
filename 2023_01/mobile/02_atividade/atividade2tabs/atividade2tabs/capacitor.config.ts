import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'atividade2tabs',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
