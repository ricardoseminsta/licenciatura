import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'aula_03_input',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
